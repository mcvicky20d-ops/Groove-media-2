# Copy this file to .env.local and paste your n8n Cloud webhook URL.
# The contact form posts leads to /api/lead, which forwards them here.
# Restart `npm run dev` after changing env vars.

N8N_WEBHOOK_URL=https://YOUR-SUBDOMAIN.app.n8n.cloud/webhook/your-webhook-id
